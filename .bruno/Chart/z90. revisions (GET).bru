meta {
  name: revisions (GET)
  type: http
  seq: 91
}

get {
  url: {{baseUrl}}/v1/clusters/{{cluster_name}}/helm-releases/{{releasesName}}/revisions
  body: none
  auth: none
}

docs {
  `GET /v1/clusters/{clusterName}/helm-releases/{releaseName}/revisions`
}
