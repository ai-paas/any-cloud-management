meta {
  name: 2. Dead Letter Messages
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/workflow/dead-letter-messages?limit=20
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
