meta {
  name: agent 재설치 (POST)
  type: http
  seq: 99
}

post {
  url: {{baseUrl}}/v1/admin/clusters/{{cluster_name}}/agent/reinstall
  body: none
  auth: none
}

docs {
  `POST /v1/admin/clusters/{clusterName}/agent/reinstall`
}
