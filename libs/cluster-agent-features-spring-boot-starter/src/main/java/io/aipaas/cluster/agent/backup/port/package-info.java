/**
 * backup sub-feature 의 SPI port — 호스트 backend 가 override 가능한 외부 통합 interface.
 *
 * <p>각 port 는 default impl 이 제공되는 경우와 호스트 책임 인 경우가 있음 — 자세히는
 * docs/architecture/starters/cluster-agent-features-starter.md 의 SPI 표 참조.
 */
package io.aipaas.cluster.agent.backup.port;
