/**
 * VmOptions 도메인의 REST API contract — CSP 별 region/spec/image catalog 의 wire DTO.
 *
 * <p>모든 type 이 *Dto suffix (조회 전용 — Request/Response 분리 불요). 5 file flat.
 */
package com.aipaas.anycloud.domain.vmoptions.api;
