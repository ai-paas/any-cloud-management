/**
 * Provisioning 도메인의 내부 value object / enum / state machine 모델.
 *
 * <p>service layer 끼리 주고 받는 type. 외부 API contract 와는 mapper 를 통해 변환. wire format 은
 * {@link com.aipaas.anycloud.domain.provisioning.api}.
 */
package com.aipaas.anycloud.domain.provisioning.model;
